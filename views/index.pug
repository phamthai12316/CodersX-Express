extends layouts/common.pug

block content
  div(class="container")
    div(class="row")
      nav(class="navbar navbar-expand-lg navbar-dark bg-dark ")
        div(class="collapse navbar-collapse", id="navbarNav")
          ul(class="navbar-nav")
            li(class="nav-item")
              a(class="nav-link", href="/users") User list
              a(class="nav-link", href="/books") Book list
              a(class="nav-link", href="/transactions") Transaction list
